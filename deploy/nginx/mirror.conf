server {
    listen 8081;
    server_name _;

    location = /healthz {
        default_type application/json;
        return 200 '{"status":"ok"}';
    }

    location /content/ {
        alias /srv/lanready/content/;
        autoindex off;
        add_header X-Content-Type-Options nosniff always;
    }
}
